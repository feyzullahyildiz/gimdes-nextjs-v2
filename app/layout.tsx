import type { Metadata } from "next";
import "./globals.css";
import { getVersions } from "@/service/get-versions";
import { Header } from "@/components/header";
import { Footer } from "@/components/footer";
import { cookies } from "next/headers";



export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default async function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  const versions = await getVersions();
  const cookieStore = await cookies();
  const activeVersion = cookieStore.get("version")?.value || versions[versions.length - 1];
  // const activeVersion = cookieStore.get("version")?.value || versions[0];
  return (
    <html lang="tr">
      <body className="min-h-screen flex flex-col">
        <Header versions={versions} activeVersion={activeVersion} />
        <div className="bg-red-300 flex-1">{children}</div>
        <Footer />
      </body>
    </html>
  );
}
